export enum UserRole {
  Administrator = 'Administrator',
  Volunteer = 'Volunteer',
  Default = 'Default',
}

export enum UserRegistrationMethod {
  Default = 'Default',
  Google = 'Google',
  Discord = 'Discord',
}
